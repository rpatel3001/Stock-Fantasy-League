from flask_restful import reqparse, abort, Resource

class Leagues(Resource):
    """
    @staticmethod  # getAllLeaguesInfo
    def get(cur):
        cur.execute("select * from userprefs;")
        return cur.fetchone()  # returning all json strings as one large json string

"""
